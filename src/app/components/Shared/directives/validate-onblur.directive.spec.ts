import { ValidateOnblurDirective } from './validate-onblur.directive';

describe('ValidateOnblurDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateOnblurDirective();
    expect(directive).toBeTruthy();
  });
});
